# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Volume Override Test' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Volume Override Test

  target 'Volume Override TestTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Volume Override TestUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
